// Re-export kept for existing import sites. The implementation now lives in
// src/lib/scoring.ts, which is the single source of truth for score banding.
export { getScoreColor } from '../lib/scoring';
